# Plan: Add Swift Package Manager support to screen_corner_radius

## Issue
GitHub issue [#4](https://github.com/ivangalkindeveloper/screen_corner_radius/issues/4) — Flutter 3.44 warns that `screen_corner_radius` doesn't support Swift Package Manager (SPM). CocoaPods registry becomes read-only Dec 2, 2026.

## Source
Forked repo: `https://github.com/exeshka/screen_corner_radius`
Cloned to: `/Users/exeshka/PROJECTS/FLUTTER_PROJECTS/screen_corner_radius`

## Current state
- Plugin is Swift-based (`ios/Classes/ScreenCornerRadiusPlugin.swift`)
- Has CocoaPods podspec (`ios/screen_corner_radius.podspec`)
- Has `PrivacyInfo.xcprivacy` in `ios/Resources/`
- `ios/Assets/` is empty (just `.gitkeep`)
- Supports iOS only (platform `12.0` in podspec)

## Changes

### 1. Create SPM directory structure
```
ios/screen_corner_radius/
  Package.swift
  Sources/
    screen_corner_radius/
      ScreenCornerRadiusPlugin.swift   (moved from ios/Classes/)
      PrivacyInfo.xcprivacy            (moved from ios/Resources/)
```

### 2. Create `ios/screen_corner_radius/Package.swift`
```swift
// swift-tools-version: 5.9
import PackageDescription

let package = Package(
    name: "screen_corner_radius",
    platforms: [
        .iOS("12.0")
    ],
    products: [
        .library(name: "screen-corner-radius", targets: ["screen_corner_radius"])
    ],
    dependencies: [
        .package(name: "FlutterFramework", path: "../FlutterFramework")
    ],
    targets: [
        .target(
            name: "screen_corner_radius",
            dependencies: [
                .product(name: "FlutterFramework", package: "FlutterFramework")
            ],
            resources: [
                .process("PrivacyInfo.xcprivacy"),
            ]
        )
    ]
)
```

### 3. Move source files
- `ios/Classes/ScreenCornerRadiusPlugin.swift` → `ios/screen_corner_radius/Sources/screen_corner_radius/`
- `ios/Resources/PrivacyInfo.xcprivacy` → `ios/screen_corner_radius/Sources/screen_corner_radius/`

### 4. Update podspec (`ios/screen_corner_radius.podspec`)
Update `source_files` path:
```ruby
s.source_files = 'screen_corner_radius/Sources/screen_corner_radius/**/*.swift'
```
Add resource_bundles (uncomment and update path):
```ruby
s.resource_bundles = {'screen_corner_radius_privacy' => ['screen_corner_radius/Sources/screen_corner_radius/PrivacyInfo.xcprivacy']}
```

### 5. Delete old empty directories
- `ios/Classes/` (now empty)
- `ios/Resources/` (now empty)
- `ios/Assets/` (was just `.gitkeep`)

### 6. Update `.gitignore`
Add:
```
.build/
.swiftpm/
```

## Verification
1. Run `flutter config --enable-swift-package-manager`
2. `cd example && flutter pub get` — should resolve without SPM warning
3. Verify podspec paths still work: `pod lib lint ios/screen_corner_radius.podspec --configuration=Debug --skip-tests`
